

function PostDay({
                     item
                 }){

    return (
        <div key={item.id} className={`post`}>
            <div className='NameBuddy'>

            </div>
        </div>
    );
}
export default PostDay;